# Ambiente
NODE_ENV=production
TZ=America/Sao_Paulo

# Segurança
# Gere um hex de 64 caracteres (ex.: `openssl rand -hex 32`)
COOKIE_SECRET=COLOQUE_AQUI_64_HEX

# Usuário MASTER inicial (criado no primeiro boot do app)
MASTER_EMAIL=provedor@provedor
MASTER_PASSWORD=0123456789
# MASTER_TOKEN=opcional_token_fixo

# Token para a rota /api/rb/<TOKEN>/send
RB_TOKEN=DEFINA_AQUI
